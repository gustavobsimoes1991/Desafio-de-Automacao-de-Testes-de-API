{
  "name": "api-tests-serverest",
  "version": "1.0.0",
  "description": "Testes automatizados API - Serverest (Desafio)",
  "scripts": {
    "test": "mocha --recursive --timeout 120000 --reporter mochawesome --reporter-options reportDir=reports,reportFilename=mochawesome,quiet=true",
    "test:ci": "npm test"
  },
  "dependencies": {
    "axios": "^1.5.0",
    "dotenv": "^16.0.0"
  },
  "devDependencies": {
    "chai": "^4.3.7",
    "mocha": "^10.2.0",
    "mochawesome": "^7.1.3"
  }
}
